/* eslint-disable */
// you may need to change the import path based on the
// values of buildDir and srcDir in your nuxt.config
import { deserializeModel, deserialize, serializeModel, isJSORMObject, handleStoreHydration } from '~/.nuxt/orm/utils'
export { deserializeModel, deserialize, serializeModel, isJSORMObject, handleStoreHydration }
